<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility service configuration for GestureAccessibilityService.

    canPerformGestures="true" — enables dispatchGesture() for touch simulation.
    This is the core capability that lets us scroll, zoom, and long-press
    on behalf of the user via hand gestures.

    flagRequestFilterKeyEvents — not used but keeps compatibility.
    We only use typeAllMask to receive all event types (not that we
    actually process any — our service only dispatches gestures).
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFlags="flagDefault"
    android:canPerformGestures="true"
    android:notificationTimeout="100" />
